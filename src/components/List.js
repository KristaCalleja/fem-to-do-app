import React from 'react';

function List({todos, setTodos, filteredTodos}){
    return(
        <div className="list">
            <ul className="unordered">
            </ul>
        </div>
    )
}

export default List;